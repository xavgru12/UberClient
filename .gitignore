bin/
obj/
.vs/
# Superseded skin art, renamed aside rather than deleted when a texture was replaced.
# Kept on disk so a regression can be compared against what it replaced; not history.
*.superseded

# icon backups taken before a re-render -- kept on disk, not in the repo
*_Icon.png.pre-water

# local safety copies from the 2026-08-17/18 skin work -- never part of a build
*.pre-glass
*.variantB
*.superseded
